# ================================
# Online Reviewer System - Environment Variables
# ================================
# Copy this file to .env and fill in your actual values
# Never commit .env to version control!

# ================================
# DATABASE CONFIGURATION (Required)
# ================================
# PostgreSQL database connection string
# Format: postgresql://username:password@host:port/database
# Example: postgresql://user:pass@localhost:5432/reviewer_db
DATABASE_URL=postgresql://username:password@host:port/database_name

# Alternative: Individual PostgreSQL connection parameters
# (Only needed if not using DATABASE_URL)
# PGHOST=localhost
# PGPORT=5432
# PGUSER=your_username
# PGPASSWORD=your_password
# PGDATABASE=reviewer_db

# ================================
# GOOGLE CALENDAR INTEGRATION (Required for Calendar Features)
# ================================
# Get these from Google Cloud Console: https://console.cloud.google.com
# 1. Create a new project or select existing
# 2. Enable Google Calendar API
# 3. Create OAuth 2.0 credentials (Web application)
# 4. Add authorized redirect URIs:
#    - http://localhost:5000/api/auth/google/callback (for development)
#    - https://your-domain.com/api/auth/google/callback (for production)

GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# ================================
# SESSION CONFIGURATION (Required)
# ================================
# Secret key for session encryption
# Generate a random string: openssl rand -base64 32
# IMPORTANT: Keep this secret and never share it!
SESSION_SECRET=your-super-secret-session-key-change-this-in-production

# ================================
# SERVER CONFIGURATION (Optional)
# ================================
# Port for the application server (default: 5000)
PORT=5000

# Node environment: development or production
NODE_ENV=development

# ================================
# REPLIT-SPECIFIC VARIABLES (Auto-configured on Replit)
# ================================
# The following variables are automatically set by Replit.
# You only need to set them if running locally or on another platform.

# Replit project ID (used for authentication)
# REPL_ID=your-repl-id

# Replit domains for OIDC redirect
# REPLIT_DOMAINS=your-repl-domain.replit.app

# OIDC issuer URL (default: https://replit.com/oidc)
# ISSUER_URL=https://replit.com/oidc

# Replit connectors hostname (for GitHub integration)
# REPLIT_CONNECTORS_HOSTNAME=connectors.replit.com

# Replit identity token (automatically set)
# REPL_IDENTITY=auto-generated-token

# Replit renewal token (automatically set)
# WEB_REPL_RENEWAL=auto-generated-token

# ================================
# SETUP INSTRUCTIONS
# ================================
# 
# 1. Copy this file:
#    cp .env.example .env
#
# 2. Fill in required values:
#    - DATABASE_URL: Your PostgreSQL connection string
#    - GOOGLE_CLIENT_ID & GOOGLE_CLIENT_SECRET: From Google Cloud Console
#    - SESSION_SECRET: Generate with: openssl rand -base64 32
#
# 3. Run database migrations:
#    npm run db:push
#
# 4. Start the application:
#    npm run dev
#
# ================================
# GETTING GOOGLE CALENDAR CREDENTIALS
# ================================
#
# Step 1: Go to Google Cloud Console
#    https://console.cloud.google.com/
#
# Step 2: Create a new project (or select existing)
#    Click "Select a project" → "New Project"
#    Name: "Online Reviewer System"
#    Click "Create"
#
# Step 3: Enable Google Calendar API
#    Go to "APIs & Services" → "Library"
#    Search for "Google Calendar API"
#    Click "Enable"
#
# Step 4: Create OAuth Credentials
#    Go to "APIs & Services" → "Credentials"
#    Click "Create Credentials" → "OAuth client ID"
#    Application type: "Web application"
#    Name: "Online Reviewer System"
#    
#    Authorized redirect URIs (add both):
#    - http://localhost:5000/api/auth/google/callback
#    - https://your-production-domain.com/api/auth/google/callback
#
#    Click "Create"
#
# Step 5: Copy credentials
#    Copy the "Client ID" → paste as GOOGLE_CLIENT_ID
#    Copy the "Client Secret" → paste as GOOGLE_CLIENT_SECRET
#
# ================================
# TROUBLESHOOTING
# ================================
#
# Database connection errors:
#   - Verify DATABASE_URL format is correct
#   - Ensure PostgreSQL server is running
#   - Check firewall allows connection to database port
#
# Google Calendar errors:
#   - Verify redirect URIs match exactly (including http/https)
#   - Ensure Google Calendar API is enabled
#   - Check credentials are copied correctly (no extra spaces)
#
# Session errors:
#   - Generate a strong SESSION_SECRET
#   - Don't use the example value in production
#
# ================================
